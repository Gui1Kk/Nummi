import { FormEvent, useState } from "react";
import { Check, PauseCircle, Pencil, PlayCircle, Trash2, X } from "lucide-react";
import { Card, EmptyState, Field } from "../../components/ui";
import { recurrenceFormSchema, subscriptionFormSchema, type RecurrenceFormInput, type SubscriptionFormInput } from "../../schemas";
import { financeService } from "../../services/finance";
import { formatDate, todayIso } from "../../lib/format";
import type { Category, RecurringRule, Subscription } from "../../types";

const recBlank=():RecurrenceFormInput=>({description:"",amount:0,kind:"expense",category_id:null,frequency:"monthly",interval_count:1,anchor_day:new Date().getDate(),start_date:todayIso(),next_date:todayIso(),end_date:null,auto_post:true,active:true,note:null});
const subBlank=():SubscriptionFormInput=>({name:"",amount:0,category_id:null,cycle:"monthly",interval_count:1,billing_day:new Date().getDate(),start_date:todayIso(),next_charge:todayIso(),end_date:null,active:true,auto_post:true,reminder_days:3,website:null,note:null});
export function AutomationsView({ rules, subscriptions, categories, money, busy, run, remove }:{rules:RecurringRule[];subscriptions:Subscription[];categories:Category[];money:(value:number)=>string;busy:boolean;run:(action:()=>Promise<unknown>,message:string)=>Promise<boolean>;remove:(action:()=>Promise<void>)=>Promise<void>}){
  const[rec,setRec]=useState<RecurrenceFormInput>(recBlank());const[recId,setRecId]=useState<string|null>(null);const[sub,setSub]=useState<SubscriptionFormInput>(subBlank());const[subId,setSubId]=useState<string|null>(null);const[error,setError]=useState("");
  const cat=(kind:"income"|"expense")=>categories.filter(c=>!c.archived&&(c.scope==="both"||c.scope===kind));
  function editRec(item:RecurringRule){setRecId(item.id);setRec({description:item.description,amount:item.amount,kind:item.kind,category_id:item.category_id,frequency:item.frequency,interval_count:item.interval_count,anchor_day:item.anchor_day,start_date:item.start_date,next_date:item.next_date,end_date:item.end_date,auto_post:item.auto_post,active:item.active,note:item.note});setError("")}
  function editSub(item:Subscription){setSubId(item.id);setSub({name:item.name,amount:item.amount,category_id:item.category_id,cycle:item.cycle,interval_count:item.interval_count,billing_day:item.billing_day,start_date:item.start_date,next_charge:item.next_charge,end_date:item.end_date,active:item.active,auto_post:item.auto_post,reminder_days:item.reminder_days,website:item.website,note:item.note});setError("")}
  async function saveRec(event:FormEvent){event.preventDefault();const parsed=recurrenceFormSchema.safeParse(rec);if(!parsed.success)return setError(parsed.error.issues[0]?.message??"Confira a recorrência.");if(await run(()=>financeService.saveRecurrence(parsed.data,recId??undefined),recId?"Recorrência atualizada.":"Recorrência criada.")){setRec(recBlank());setRecId(null)}}
  async function saveSub(event:FormEvent){event.preventDefault();const parsed=subscriptionFormSchema.safeParse(sub);if(!parsed.success)return setError(parsed.error.issues[0]?.message??"Confira a assinatura.");if(await run(()=>financeService.saveSubscription(parsed.data,subId??undefined),subId?"Assinatura atualizada.":"Assinatura criada.")){setSub(subBlank());setSubId(null)}}
  return <div className="two automation-grid"><Card title={recId?"Editar recorrência":"Nova recorrência"} subtitle="Para salário, aluguel, contas e qualquer repetição"><form className="grid compact-grid" onSubmit={saveRec}><Field label="Descrição"><input value={rec.description} onChange={e=>setRec({...rec,description:e.target.value})} required/></Field><Field label="Valor"><input type="number" min="0.01" step="0.01" value={rec.amount||""} onChange={e=>setRec({...rec,amount:e.target.valueAsNumber})} required/></Field><Field label="Tipo"><select value={rec.kind} onChange={e=>setRec({...rec,kind:e.target.value as "income"|"expense",category_id:null})}><option value="expense">Saída</option><option value="income">Entrada</option></select></Field><Field label="Categoria"><select value={rec.category_id??""} onChange={e=>setRec({...rec,category_id:e.target.value||null})}><option value="">Sem categoria</option>{cat(rec.kind).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></Field><Field label="Frequência"><select value={rec.frequency} onChange={e=>setRec({...rec,frequency:e.target.value as RecurrenceFormInput["frequency"]})}><option value="daily">Diária</option><option value="weekly">Semanal</option><option value="monthly">Mensal</option><option value="yearly">Anual</option></select></Field><Field label="A cada"><input type="number" min="1" max="365" value={rec.interval_count} onChange={e=>setRec({...rec,interval_count:e.target.valueAsNumber})}/></Field><Field label="Próxima ocorrência"><input type="date" value={rec.next_date} onChange={e=>setRec({...rec,next_date:e.target.value,start_date:recId?rec.start_date:e.target.value})}/></Field><Field label="Dia-base"><input type="number" min="1" max="31" value={rec.anchor_day} onChange={e=>setRec({...rec,anchor_day:e.target.valueAsNumber})}/></Field><label className="check"><input type="checkbox" checked={rec.auto_post} onChange={e=>setRec({...rec,auto_post:e.target.checked})}/>Criar lançamento automaticamente</label>{error&&<p className="form-error">{error}</p>}<div className="form-actions"><button className="primary" disabled={busy}><Check size={16}/>{recId?"Salvar":"Criar"}</button>{recId&&<button type="button" className="secondary" onClick={()=>{setRec(recBlank());setRecId(null)}}><X size={16}/>Cancelar</button>}</div></form>{rules.length?<ul className="list automation-list">{rules.map(item=><li key={item.id}><div><b>{item.description}</b><small>{money(item.amount)} · próxima em {formatDate(item.next_date)} · {item.active?"ativa":"pausada"}</small></div><div className="row-actions"><button className="icon" onClick={()=>run(()=>financeService.patchRecurrence(item.id,{active:!item.active}),item.active?"Recorrência pausada.":"Recorrência ativada.")} aria-label={item.active?"Pausar":"Ativar"}>{item.active?<PauseCircle size={16}/>:<PlayCircle size={16}/>}</button><button className="icon" onClick={()=>editRec(item)}><Pencil size={16}/></button><button className="icon danger-icon" onClick={()=>remove(()=>financeService.deleteRecurrence(item.id))}><Trash2 size={16}/></button></div></li>)}</ul>:<EmptyState title="Sem recorrências" description="Crie uma regra para não digitar o mesmo lançamento todo mês."/>}</Card>
  <Card title={subId?"Editar assinatura":"Nova assinatura"} subtitle="Acompanhe serviços e datas de cobrança"><form className="grid compact-grid" onSubmit={saveSub}><Field label="Serviço"><input value={sub.name} onChange={e=>setSub({...sub,name:e.target.value})} required/></Field><Field label="Valor"><input type="number" min="0.01" step="0.01" value={sub.amount||""} onChange={e=>setSub({...sub,amount:e.target.valueAsNumber})} required/></Field><Field label="Categoria"><select value={sub.category_id??""} onChange={e=>setSub({...sub,category_id:e.target.value||null})}><option value="">Sem categoria</option>{cat("expense").map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></Field><Field label="Ciclo"><select value={sub.cycle} onChange={e=>setSub({...sub,cycle:e.target.value as "monthly"|"yearly"})}><option value="monthly">Mensal</option><option value="yearly">Anual</option></select></Field><Field label="Próxima cobrança"><input type="date" value={sub.next_charge} onChange={e=>setSub({...sub,next_charge:e.target.value,start_date:subId?sub.start_date:e.target.value})}/></Field><Field label="Dia da cobrança"><input type="number" min="1" max="31" value={sub.billing_day} onChange={e=>setSub({...sub,billing_day:e.target.valueAsNumber})}/></Field><Field label="Avisar antes"><input type="number" min="0" max="60" value={sub.reminder_days} onChange={e=>setSub({...sub,reminder_days:e.target.valueAsNumber})}/></Field><Field label="Site"><input type="url" value={sub.website??""} onChange={e=>setSub({...sub,website:e.target.value||null})} placeholder="https://"/></Field><label className="check"><input type="checkbox" checked={sub.auto_post} onChange={e=>setSub({...sub,auto_post:e.target.checked})}/>Criar lançamento automaticamente</label><div className="form-actions"><button className="primary" disabled={busy}><Check size={16}/>{subId?"Salvar":"Criar"}</button>{subId&&<button type="button" className="secondary" onClick={()=>{setSub(subBlank());setSubId(null)}}><X size={16}/>Cancelar</button>}</div></form>{subscriptions.length?<ul className="list automation-list">{subscriptions.map(item=><li key={item.id}><div><b>{item.name}</b><small>{money(item.amount)} · {formatDate(item.next_charge)} · {item.active?"ativa":"pausada"}</small></div><div className="row-actions"><button className="icon" onClick={()=>run(()=>financeService.patchSubscription(item.id,{active:!item.active}),item.active?"Assinatura pausada.":"Assinatura ativada.")}>{item.active?<PauseCircle size={16}/>:<PlayCircle size={16}/>}</button><button className="icon" onClick={()=>editSub(item)}><Pencil size={16}/></button><button className="icon danger-icon" onClick={()=>remove(()=>financeService.deleteSubscription(item.id))}><Trash2 size={16}/></button></div></li>)}</ul>:<EmptyState title="Sem assinaturas" description="Cadastre streaming, internet, nuvem e outros serviços recorrentes."/>}</Card></div>;
}
